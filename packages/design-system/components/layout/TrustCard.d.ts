import {ReactNode, ReactElement } from 'react';
/** @startingPoint section="Layout" subtitle="Icon + claim card for trust / security grid" viewport="360x240" */
export interface TrustCardProps { icon?: ReactNode; title: ReactNode; children: ReactNode; }
export declare function TrustCard(props: TrustCardProps): ReactElement;
