import { ReactElement } from 'react';
/** @startingPoint section="Feedback" subtitle="Monospace hash / run-ID chip with copy affordance" viewport="480x120" */
export interface EvidenceHashProps {
  label?: string;
  value: string;
  truncate?: boolean;
}
export declare function EvidenceHash(props: EvidenceHashProps): ReactElement;
