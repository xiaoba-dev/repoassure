import {ReactNode, ReactElement } from 'react';
/** @startingPoint section="Layout" subtitle="Horizontal rule, optionally with centered label" viewport="520x120" */
export interface DividerProps { label?: ReactNode; spacing?: number; }
export declare function Divider(props: DividerProps): ReactElement;
