# Security Assurance Lane Spec v0.1

Status: Draft
Date: 2026-06-23
Source ADR: [ADR-0013](../../adr/0013-codex-security-and-security-assurance-lane.md)

## Purpose

Define the first product and architecture contract for importing security evidence into RepoAssure without repositioning RepoAssure as a generic vulnerability scanner.

RepoAssure remains the repo readiness and delivery assurance layer. Security tools provide provider-specific findings; RepoAssure normalizes those findings into local artifacts, repair planning inputs, and AI IDE / Agent handoff material.

This spec explicitly covers Provider provenance, Local-first evidence handling, Redaction requirements, Artifact layout, and Repair plan and repair task package integration.

## Positioning

Codex Security is the preferred first provider because it already models the expected security scan lifecycle: threat model, finding discovery, validation, attack-path analysis, report generation, and finding tracking.

RepoAssure should still support a provider-neutral lane:

```text
Security Assurance Lane
  Provider adapters
    codex-security
    codeql
    semgrep
    gitleaks
    osv
    browser-security
    manual-import
  Evidence normalization
  RepoAssure artifacts
  Repair plan integration
  Agent handoff
```

## Provider Interface

A provider adapter should convert provider-native output into a normalized security evidence bundle.

Minimal provider contract:

| Field | Required | Description |
| --- | --- | --- |
| `provider` | Yes | Stable provider id, for example `codex-security` |
| `providerVersion` | No | Provider or plugin version when available |
| `sourceType` | Yes | `scan-dir`, `sarif`, `json`, `markdown`, or `manual` |
| `sourcePath` | Yes | Local path to imported provider output |
| `importedAt` | Yes | ISO timestamp generated by RepoAssure |
| `targetRepoRoot` | Yes | Local target repo root or redacted equivalent in exported reports |
| `targetRevision` | No | Git commit or provider target revision when available |
| `findings` | Yes | Normalized finding list |
| `coverage` | No | Provider coverage summary or limitations |
| `rawArtifacts` | No | Local references to retained provider artifacts |

Future CLI shape:

```bash
repoassure security import --provider codex-security --scan-dir <scan-dir>
```

The current implementation name may expose this through `hardening` first, but user-facing docs should keep the RepoAssure product language.

## Normalized Finding Contract

Each imported security finding should preserve provider provenance and map into RepoAssure repair planning.

| Field | Required | Description |
| --- | --- | --- |
| `findingId` | Yes | Stable RepoAssure finding id |
| `providerFindingId` | Yes | Provider-native id or deterministic imported id |
| `provider` | Yes | Source provider id |
| `title` | Yes | Short finding title |
| `severity` | Yes | Normalized `P0` / `P1` / `P2` / `P3` or mapped provider severity |
| `category` | Yes | Security category, CWE, advisory family, or provider category |
| `affectedLocations` | Yes | Path and optional line range list |
| `evidence` | Yes | Redacted evidence snippets and artifact references |
| `attackPath` | No | Provider attack-path summary when available |
| `validationStatus` | No | `validated`, `suspected`, `suppressed`, `not_applicable`, or `deferred` |
| `remediation` | No | Provider or RepoAssure remediation guidance |
| `verification` | No | Suggested verification commands or manual checks |

## Provider Provenance

Provider provenance must survive every transformation:

- report output
- `findings.json`
- `repair-plan.json`
- `repair-task-package.json`
- handoff packages
- patch plans

RepoAssure must not rewrite provider evidence in a way that makes it appear natively discovered by RepoAssure. Provider ids, source artifact paths, scan timestamps, target revision, and known limitations should remain visible in machine-readable artifacts.

## Local-First Evidence Handling

Security evidence import is local-first:

- Read provider output from local files or explicit local scan directories.
- Do not upload target code, provider reports, screenshots, traces, logs, env values, or secrets.
- Do not fetch remote provider state unless a future ADR explicitly approves a remote integration.
- Store normalized artifacts in the target repo hardening run bundle or a workspace output directory.
- Preserve raw provider artifacts by local reference where possible instead of copying large sensitive files into this repo.

## Redaction Requirements

All imported provider text is untrusted and may contain secrets. Before writing RepoAssure-owned reports or terminal/MCP output, apply the shared redaction boundary to:

- API keys and provider tokens
- Authorization and Cookie headers
- session, CSRF, JWT, OAuth, and signed URL values
- URL userinfo and sensitive query or fragment parameters
- local paths when rendered in user-facing handoff commands
- provider evidence snippets
- remediation text copied from provider reports

Raw provider artifacts may remain in their original provider scan directory, but RepoAssure-generated artifacts must be redacted.

## Artifact Layout

Future run-scoped layout:

```text
<target>/
  .hardening/
    runs/
      <run-id>/
        security/
          security-summary.json
          security-findings.json
          providers/
            codex-security/
              import-manifest.json
              normalized-findings.json
        findings.json
        repair-plan.json
        repair-task-package.json
        repair-handoff-package.json
```

`security-summary.json` should summarize provider count, finding count, highest severity, validation status counts, coverage limitations, and import timestamp.

`security-findings.json` should contain only normalized, redacted findings plus local references to provider artifacts.

## Repair Plan and Repair Task Package Integration

Security findings should become repair planning inputs using the same task machinery as browser, CLI, architecture, and test findings.

Repair tasks generated from security evidence should include:

- provider provenance
- affected locations
- redacted evidence
- attack-path or validation summary when available
- remediation intent
- verification commands or manual verification notes
- explicit uncertainty when provider validation is incomplete

Security tasks should not automatically outrank runtime P0 failures unless severity mapping and exploitability evidence justify it. The first implementation should use conservative prioritization and keep provider severity visible.

## Acceptance and Goal Audit

When implemented, goal audit should verify:

- security imports are local-first
- provider provenance exists
- generated artifacts are redacted
- security findings can feed repair plan and repair task package outputs
- unsupported provider formats fail with structured blockers

Security imports should not be required for the current MVP acceptance gate. They are an optional future lane unless a later ADR changes the product scope.

## Non-goals

- This spec defines no native deep vulnerability scanner for the current MVP.
- No native deep vulnerability scanner in the current MVP.
- No replacement for Codex Security, CodeQL, Semgrep, Gitleaks, OSV, or customer-mandated scanners.
- No automatic remote upload of provider artifacts.
- No automatic creation of security advisories, issues, PRs, or external tracker records.
- No weakening of existing browser or Python/CLI acceptance modes.
- No target repo source modification in the import step.

## Follow-up

- Write a provider import implementation goal after this spec is accepted.
- Start with a Codex Security local scan directory importer.
- Add fixture provider outputs before runtime implementation.
- Extend repair-plan tests with security evidence types.
- Update goal audit only when provider import becomes implemented behavior.
