# Several scripts run a compiled entry point directly (`node packages/acceptance/dist/...`)
# and rely on a `pre*` hook to build first. pnpm runs those hooks only when this is on,
# and the default has differed across pnpm majors, so pin it rather than inherit it.
# Without it those commands render a verdict from whatever dist happens to be present.
enable-pre-post-scripts=true
