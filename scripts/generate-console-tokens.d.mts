/* Type side for the generator, so tests/unit/console-design-tokens.test.ts can call it
   directly and compare against the checked-in module rather than shelling out. */
export declare function buildConsoleTokensModule(): Promise<string>;
