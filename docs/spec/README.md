# Spec Docs

Status: routing_index

This directory is reserved for future solution specs such as CLI, MCP, web, API, runtime, and integration contracts.

Current detailed specs remain under `docs/product/specs/` and `docs/architecture/specs/`; `docs/SPEC.md` is the canonical thin entrypoint.

