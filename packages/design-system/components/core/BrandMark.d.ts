import { ReactElement } from 'react';
/** @startingPoint section="Core" subtitle="Hexagonal assurance seal — glyph or full lockup" viewport="500x120" */
export interface BrandMarkProps {
  size?: number;
  lockup?: boolean;
  color?: string;
}
export declare function BrandMark(props: BrandMarkProps): ReactElement;
